import styled from "styled-components";

const Form = styled.form`
  .containerInputs {
    width: 100%;
    display: flex;
    flex-direction:  column;
    gap: 1rem;
  }

  button {
    margin-top: 15px;
  }
`;

export default Form;
